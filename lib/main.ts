import { color } from "./color";
import { spacing } from "./spacing";
import { text } from "./text";

const theme = {
  color,
  spacing,
  text
};

export { color };

export { spacing };

export { text };

export default theme;
